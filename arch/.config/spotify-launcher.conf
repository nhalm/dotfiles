# Wayland rather than XWayland, which is scaled at 1.25 and looks soft.
# Check with: spotify-launcher -v --skip-update --no-exec
[spotify]
extra_arguments = ["--enable-features=UseOzonePlatform", "--ozone-platform=wayland"]
