# Settings only. Every tool lives in conf.d/ -- shared.toml for both machines,
# darwin.toml for macOS -- so the full set is readable in one directory.
[settings]
idiomatic_version_file_enable_tools = ["node"]
auto_install = true
not_found_auto_install = true
